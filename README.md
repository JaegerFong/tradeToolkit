# tradeToolkit

tradeToolkit is a FastAPI and Vue based toolkit for market data, stock analysis,
screening, reporting, and web workflows.

This root README is also used as package metadata by `pyproject.toml` during
Docker image builds. For setup and usage details, see `docs/QUICK_START.md` and
the deployment guides under `docs/deployment/`.
